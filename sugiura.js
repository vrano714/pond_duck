var d;

var a;

var c;

var b;

var e;

var f;

var g;

var h;

var i;

var j;

var k;

var l;



while (true) {

  if (loc_x() <= 40) {

    swim(0);

  } else if (loc_x() >= 60) {

    swim(180);

  }

  d = scan(315);

  if (d <= 80) {

    cannon(315, d);

  }

  a = scan(270);

  if (a <= 80) {

    cannon(270, a);

  }

  c = scan(225);

  if (c <= 80) {

    cannon(225, c);

  }

  b = scan(180);

  if (b <= 80) {

    cannon(180, b);

  }

  e = scan(0);

  if (e <= 80) {

    cannon(0, e);

  }

  f = scan(135);

  if (f <= 80) {

    cannon(135, f);

  }

  g = scan(90);

  if (g <= 80) {

    cannon(90, g);

  }

  h = scan(45);

  if (h <= 80) {

    cannon(45, h);

  }

  i = scan(165);

  if (i <= 80) {

    cannon(165, i);

  }

  j = scan(195);

  if (j <= 80) {

    cannon(195, j);

  }

  k = scan(15);

  if (k <= 80) {

    cannon(15, k);

  }

  l = scan(345);

  if (l <= 80) {

    cannon(345, l);

  }
}

